%h3 最新发表列表
%table.table
  %tr
    %th 标题
    %th 内容
    %th 发表者
    %th 发表日期
  - @posts.try(:each) do |post|
    %tr
      %td= link_to post.title, post_path(post)
      %td= post.content
      %td= post.user.name
      %td= post.created_at
  = paginate @posts

- if login?
  = link_to 'new Post', new_user_post_path(current_user), class: 'btn-create'
